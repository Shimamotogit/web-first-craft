# Security and privacy

## データの流れ

### 通常の編集

- 入力内容と縮小済み写真は、利用中ブラウザの `localStorage` に保存されます。
- 解析、広告、外部フォント、外部APIへの送信はありません。
- 書き出した自己紹介ページにはJavaScriptと外部リソースを含めません。

### QR写真転送

- パソコンが一時トークンを発行します。
- スマートフォンは同じLAN上のパソコンへ縮小済み画像を送ります。
- 画像はサーバープロセスのメモリだけに保持され、ディスクには保存されません。
- セッションは約20分で失効します。
- 作成画面が画像を取得すると、サーバー側の画像欄は即座に空になります。

### 完成HTML・作品カードPNGのQR共有

- 完成HTMLと作品カードPNGはサーバープロセスのメモリだけに保持されます。
- 共有URLは推測困難なランダムトークンを含みます。
- セッションは約30分で失効します。
- サーバー終了時には全セッションが消えます。
- HTML閲覧レスポンスには、外部通信とスクリプト実行を制限するContent Security Policyを付けます。
- こどもモードの作品カードは、編集プレビューと同じSVGをPNG化して生成します。

## 想定している利用環境

- 家庭、教室、ワークショップなど、参加者を把握できるプライベートLAN
- パソコンとスマートフォンが同じWi‑Fiに接続された状態
- インターネットから到達できない構成

## 想定していない利用環境

- 公衆Wi‑Fiや、不特定多数が参加するネットワーク
- ルーターでのポート開放
- リバースプロキシ等を使ったインターネット公開
- HTTPSが必須となる機密情報の交換
- 長期間のファイル保管やユーザー認証

このサーバーにはログイン機能がありません。QRコードまたは共有URLを見られる人は、有効時間内であれば対象データへアクセスできます。QRコードを受け取り鍵として扱ってください。

## 子どもの安全に関する限界

簡易チェックは補助機能であり、安全を保証しません。次のような情報は見落とす可能性があります。

- 本名、珍しい愛称、顔、制服、名札
- 自宅や学校の外観、窓の反射、位置が推測できる背景
- 通学経路、習い事の曜日、よく行く場所
- 画像の中に写り込んだ文字
- 複数の情報を組み合わせた特定

配布前には、保護者、先生、運営者が文章と画像を目視確認してください。

## 脆弱性の報告

実在する子どものデータ、顔写真、連絡先、未公開ページをIssueへ添付しないでください。再現には架空のテストデータだけを使用してください。
