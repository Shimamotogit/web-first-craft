# 調査と設計判断

初回調査: 2026-07-27  /  2モード再設計・こども静止画版: 2026-08-12

## 1. プロジェクトの狙い

コンセプトは「技術で自分のページを作る。形にする仕事を体感する」。共通Mainから学習者に合わせて2モードへ分岐する。

- こどもモード: 3〜7歳程度。自己紹介を題材に、HTML / CSS / JavaScript を「なかみ / みため / ボタンでしあげ」として体験する。
- チャレンジモード: 小学校高学年〜大人。お題に合うサイトを作り、HTML / CSS / JavaScript の3軸で採点する。

コードを暗記させることより、「選んだ内容が形になる」「同じ中身でも見た目を変えられる」「ボタン操作で画面を書き換えられる」という役割の違いを体感させることを優先した。

## 2. 参考にした考え方

### Scratch

子ども自身が選び、すぐ結果を見て、試し直せる制作体験を参考にした。完成物をAIが自動生成するのではなく、本人の選択を積み上げて作品にする。

- https://scratch.mit.edu/about

### Code.org / Web Lab系の学習パターン

操作とプレビューを近くに置き、変更結果をすぐ確認できる構成を参考にした。幼児向けではコード入力を要求せず、HTML / CSS / JavaScript の名称だけは残して役割を日本語で併記する。

- https://code.org/

### WCAG 2.2

タッチ操作しやすい大きな対象、明確なフォーカス、意味の分かるラベルを基本にする。

- https://www.w3.org/TR/WCAG22/

## 3. こどもモードの設計

### STEP 1 / HTML = なかみ

- 顔写真またはイラストマーク
- なまえ
- すきなもの（最大3つ）
- ひとこと

文字入力は画面内ひらがなパッドだけで完結できる。ひらがなパッドは常時表示を強制せず、「だす / かくす」を切り替えられる。入力対象を押したときは必要に応じて再表示する。

写真はPCのファイル選択に加え、LAN内QRからスマホで撮影・選択して送れる。

### STEP 2 / CSS = みため

完成物が1枚の画像になるため、アニメーションではなく静止画の差を大きくする。

- 4テーマ: おひさま / そら / もり / キャンディ
- 4レイアウト: どーん / カード / じゆうちょう / ポスター
- 4装飾: ほし / てんてん / テープ / なし

レイアウトでは単なる色替えにせず、写真位置、名前の大きさ、ひとこと領域、好きなものの並び、用紙形状を変える。

### STEP 3 / JavaScript = ボタンでしあげ

最終画像に動きは入れない。JavaScriptは「ボタンを押したら画面の状態が変わる」役割として体験させる。

- 4写真枠: テープ / まる / きって / はじける
- 4大スタンプ: ほし / ハート / にじ / ロケット
- 4背景模様: パラパラ / チェック / なみなみ / おおきなまる

静止画デザインだけで 4 × 4 × 4 × 4 × 4 × 4 の組み合わせを持つ。

## 4. プレビューと保存画像を一致させる

以前は「編集プレビュー」と「持ち帰り用カード」を別の描画処理で作っていたため、完成時に構図が変わる問題があった。

現在は `buildKidSvg()` を唯一の完成描画ソースにしている。

- 編集プレビュー: `buildKidSvg()` → SVG Blob → `<img>`
- PC保存PNG: `buildKidSvg()` → Canvas 900×1200 → PNG
- QR共有PNG: 同じCanvas PNGを `/api/cards` へ一時送信
- おとなと保存するHTML: `buildKidSvg()` をそのままHTMLへ埋め込み

ブラウザ検証では、同じ状態から作ったプレビューの900×1200 PNGと完成Canvas PNGのSHA-256が一致することを確認した。

## 5. LAN内QR写真転送

静的HTMLだけでは別端末間でデータを渡せないため、Python標準ライブラリの `ThreadingHTTPServer` を使う。

1. PCが一時写真セッションを作成
2. ローカルサーバーがスマホ用URLをQR化
3. スマホで `<input type="file" accept="image/*" capture="user">` から撮影・選択
4. ブラウザ内で画像を縮小
5. 同一LAN上のPCへData URLをPOST
6. PC編集画面がポーリングして受信し、プレビューへ反映

`capture="user"` は対応端末で前面カメラを希望する指定だが、ブラウザ差があるため通常のファイル選択も可能にしている。

- https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/capture

## 6. LAN内作品カード受け取り

こどもモードの主要成果物はHTMLファイルではなく900×1200pxのPNGカードにする。スマホでQRを読み、受取ページから画像を表示・保存できる。

- QRはサーバー内でSVG生成
- 作品カードは約30分で失効
- 写真セッションは約20分で失効
- どちらもクラウドやDBへ保存しない
- サーバー終了時に全一時データが消える

Pythonの `http.server` は本番インターネット公開用ではないため、家庭・教室など管理されたLANだけを想定する。

- https://docs.python.org/ja/3/library/http.server.html
- https://pypi.org/project/qrcode/

## 7. チャレンジモード

大人版は「自己紹介を高機能化」ではなく、お題に応えるWeb制作体験とする。

- HTML 40点: 情報設計・必要な内容
- CSS 35点: 視覚設計・カスタマイズ
- JavaScript 25点: 操作体験

EASY / NORMAL / HARDで要求数を変え、見た目の好みではなく観測可能な条件で採点する。

## 8. 実装上の原則

- Vanilla HTML / CSS / JavaScript
- 外部CDN・Webフォントなし
- QR生成ライブラリはライセンスとともに同梱
- 子ども版の完成画像はアニメーションなし
- 画像はブラウザ内で縮小
- 表示テキストはSVG / HTMLへ入れる前にエスケープ
- LAN一時共有はメモリ保持＋期限切れ
- ルーターのポート開放やインターネット公開は想定しない
